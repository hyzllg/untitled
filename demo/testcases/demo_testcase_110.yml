
config:
    name: "request methods testcase: reference testcase"
    base_url: ${ENV(URL)}
    variables:
        - pass: ${bankcard()}

teststeps:
-
    name: post form data
    request:
        method: POST
        url: /queryAmount
        data:
          {pas: $pass}

    validate:
        - eq: ["status_code", 200]

