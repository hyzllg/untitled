import Collect
for i in range(2):
    random__name = Collect.random_name()
    generate__ID = Collect.id_card().generate_ID()
    HB_phone = Collect.phone()
    print(random__name,generate__ID,HB_phone)